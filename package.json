{
  "name": "demo",
  "version": "1.0.0",
  "description": "A DEMO Electron application",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager ./ --win --out demo --arch=x64 --version 0.1 --overwrite --ignore=node_modules"
  },
  "devDependencies": {
    "electron": "^2.0.2",
    "electron-packager": "^8.6.0"
  }
}
